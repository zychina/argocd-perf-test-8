apiVersion: v1
kind: Service
metadata:
  name: perf-test8-app833-svc
  namespace: clawtest
  labels:
    app: perf-test8-app833
spec:
  selector:
    app: perf-test8-app833
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
