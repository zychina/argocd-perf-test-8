apiVersion: v1
kind: Service
metadata:
  name: perf-test8-app640-svc
  namespace: clawtest
  labels:
    app: perf-test8-app640
spec:
  selector:
    app: perf-test8-app640
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
