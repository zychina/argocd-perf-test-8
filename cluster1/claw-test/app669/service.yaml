apiVersion: v1
kind: Service
metadata:
  name: perf-test8-app669-svc
  namespace: clawtest
  labels:
    app: perf-test8-app669
spec:
  selector:
    app: perf-test8-app669
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
