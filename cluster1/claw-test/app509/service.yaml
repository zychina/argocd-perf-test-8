apiVersion: v1
kind: Service
metadata:
  name: perf-test8-app509-svc
  namespace: clawtest
  labels:
    app: perf-test8-app509
spec:
  selector:
    app: perf-test8-app509
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
